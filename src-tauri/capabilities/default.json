{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "remote": {
    "urls": [
      "http://localhost:4000",
      "http://localhost:4000/*"
    ]
  },
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
